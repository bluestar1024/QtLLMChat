# 数据与配置

## 配置文件（config.txt）

**位置**：作为资源打包于 `:/config/config.txt`，运行时通过 `SettingWidget::loadConfig()` 读取；文件不存在时以默认值创建。

**格式**：UTF-8 文本，每行一个参数，共 6 行：

| 行号 | 参数 | 默认值 | 控件 |
| --- | --- | --- | --- |
| 1 | baseUrl（OpenAI 兼容接口地址） | `http://127.0.0.1:11434/v1` | `SettingEdit` |
| 2 | apiKey | `EMPTY` | `SettingEdit` |
| 3 | model | `deepseek-r1:1.5b` | `SettingEdit` |
| 4 | maxTokens | `5000` | `SpinBox` + `Slider` |
| 5 | topP | `0.8` | `DoubleSpinBox` + `Slider` |
| 6 | temperature | `0.8` | `DoubleSpinBox` + `Slider` |

**参数范围**（`globalvariables.h` / `mainwindow.cpp` 顶部常量）：

- maxTokens：`0 ~ 32768`
- topP：`0 ~ 1`（步进 0.01）
- temperature：`0.01 ~ 1`（步进 0.01）

**保存链路**：控件值变化 → `SettingWidget` 信号（`baseUrlTextChanged` 等）→ `MainWindow` 槽函数 → 写回 `config.txt`。

## 聊天记录（chatrecords/）

**位置**：可执行文件相对路径 `../../chatrecords/`（全局常量 `chatRecordsDir`）。

**文件命名**：`chat_YYYY_MM_DD_HH_MM_SS.txt`（创建时间）。

**格式**：UTF-8 文本，每条消息固定 3 行：

```
<消息文本（原始 Markdown，含 <think> 块）>
消息部件思考时长:N秒
True|False        ← True=用户消息，False=AI 消息
```

**写入**：`MainWindow::writeToChatRecordFile()` 遍历 `chatShow` 列表，将每条 `MessageWidget` 的文本、思考时长、是否用户逐条追加写入。

**回放**：`MainWindow::generateCurChatRecord()` 按 3 行一组解析文件，重建 `MessageWidget` 列表（`thinkTimeIndex`、`expandIndex` 恢复思考块状态）。

**管理**：`ChatRecordsWidget` 提供搜索（`LineEdit` 过滤文件名/内容）、清空全部（`clearAllChatRecords`）、点击加载历史会话。

## 代码主题与语法高亮配置

`config/` 目录下 XML 文件（均打包进 qrc）：

| 文件 | 用途 |
| --- | --- |
| `dark_theme.xml` | 代码深色主题（默认，全局 `codeThemeFilePath`） |
| `light_theme.xml` | 代码浅色主题（`CodeShow::toggleThemeStyle` 切换） |
| `cpp.xml` / `python.xml` / `lua.xml` / `glsl.xml` | 各语言语法高亮规则 |

## 资源（resources.qrc）

统一前缀 `/`，主要分组：

- `images/`：界面图标（按钮、主题、聊天记录、发送、思考等 30+ 张 PNG）。
- `font/msyhl.ttc`：微软雅黑 Light 字体（全局默认字体，`fontFilePath`）。
- `config/`：配置文件与高亮/主题 XML。
- `mathjax/`：MathJax 3 es5 全套（tex-mml-chtml 入口 + 扩展 + 字体 + 映射表）。

## WebEngine 缓存

- 全局配置：`webEngineCacheDir = "../../webengine_cache"`，`QWebEngineProfile *sharedProfile`（`globalvariables.h`）供所有 WebEngine 页面共享。
- 运行时可清理 `qt_chat/webengine_cache/` 目录解决页面异常。

## 全局常量速查（mainwindow.cpp 顶部定义）

| 常量 | 值/说明 |
| --- | --- |
| `imagesDir` | `:/images` |
| `fontFilePath` | `:/font/msyhl.ttc` |
| `configFilePath` | `:/config/config.txt` |
| `mathjaxScriptPath` | `mathjax/es5/tex-mml-chtml.js` |
| `chatRecordsDir` | `../../chatrecords/` |
| `codeThemeFilePath` | `:/config/dark_theme.xml`（可变） |
| `webEngineCacheDir` | `../../webengine_cache` |
| `windowFontPointSize` / `titleFontPointSize` / `buttonFontPointSize` | 10 / 14 / 9 |
| `initBaseUrl` / `initApiKey` / `initModel` | 默认 API 参数 |

## 相关文档

- [项目架构](项目架构.md)：各模块职责与数据流。
